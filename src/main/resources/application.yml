server:
  port: 1005

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/contract_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: eng123
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 50MB

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: org.huhu.contract.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

myminio:
  endpoint: http://localhost:9000
  accessKey: QqWDatJ1dbIKT0tbGkIy
  secretKey: tZF2d2OV4spv4gy37bynEHIjMYS26bDoKrY7Ze8s

python:
  # Python 合同识别服务地址（FastAPI：POST /api/v1/contract/recognize）
  ocr-url: http://localhost:8000/api/v1/contract/recognize

file:
  upload-dir: D:/contract-uploads/

